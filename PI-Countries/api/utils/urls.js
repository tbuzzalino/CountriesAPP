const ALL_COUNTRIES = `https://restcountries.eu/rest/v2/all`
const COUNTRIES_NAME = `https://restcountries.eu/rest/v2/name/{name}`
const COUNTRIES_CODE = `https://restcountries.eu/rest/v2/alpha/{code}`

module.exports = {
    ALL_COUNTRIES,
    COUNTRIES_NAME,
    COUNTRIES_CODE,
}